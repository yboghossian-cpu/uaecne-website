# Open Questions

Living log of content/asset gaps and unresolved items, per PROJECT_BRIEF.md rule 6. Nothing here gets filled with invented content — it stays flagged until confirmed.

## Unresolved

1. **Armenian-language content — none found anywhere in the UNION folder, with one confirmed exception.** The Wix staging site's footer renders the HQ address bilingually (English + Armenian); the Armenian address text is now resolved (see below). No broader Armenian site version exists to draw from beyond that: a direct check of `/hy` on the staging site returned a 404, so there's no full Armenian page to extract further content from. Still blocks all other ՀԱՅ-side content project-wide per rule 2 (including the org name, still needed — see below) until real source text is found elsewhere.

2. **Armenian org name — exact text still needed directly from Yeghia; a second `WebFetch` attempt, 2026-08-18, does not change this.** Re-fetched at Yeghia's request: `Մերձաւոր Արեւելքի Հայ Աւետարանական Եկեղեցիներու Միութիւն, Հիմն. 1846` (matches the earlier fetch word-for-word) plus a new fragment, `ՄԱՀԱԵՄ — Կեդրոնական Գրասենեակ` (possibly an abbreviation + "Central Office" label). The tool self-reported "moderate confidence (approximately 70–75%)" on this attempt — explicitly not reliable enough to treat as resolved, consistent with why the item was flagged in the first place. Not used anywhere in the codebase. **A repeat fetch does not change the underlying accuracy problem** — still needs the exact Unicode text via direct copy-paste or a source doc from Yeghia, same as how the address was actually resolved (see Resolved).

3. **Resource Center and Ministries nav-dropdown contents — Wix and legacy disagree; not yet wired into `Nav.tsx` (only the six top-level labels are).** Fresh direct re-parse of the legacy file (`uaecne-legacy-welcome.html`), 2026-08-18, superseding the earlier WebFetch-only check:
   - **Resource Center — legacy:** History of the UAECNE, Armenian Protestantism in the Near East, News & Press Releases, Calendar. **Wix (WebFetch, earlier):** General Assembly, Archives, History of the UAECNE. Only "History of the UAECNE" overlaps — substantially different lists.
   - **Ministries — legacy:** Churches, Schools, Higher Education, Outreach, Health Care, Publications, Cultural, Countries (8, matches the site-scope inventory exactly). **Wix (WebFetch, earlier):** Churches, Schools, Higher Education, Outreach only (4) — Wix's nav dropdown simply hasn't built out Health Care/Publications/Cultural/Countries yet.
   Per the Content & Design Strategy sourcing waterfall, legacy is the established authority for page *scope* — when dropdown UI for these two menus eventually gets built, the legacy/inventory lists should win over the narrower Wix dropdown data. Still not built into `Nav.tsx` either way; needs a decision at that point, not before.

4. **Per-school `logo.jpg` files — approval status unconfirmed.** e.g. `Schools/Shamlian Tatigian/logo.jpg`. Currently assumed to be that school's own logo, scoped to its own page, distinct from the site-wide seal (`uaecne-symbol.svg`). Not yet confirmed as approved/current assets by Yeghia.

5. **HQ address/phone/email — legacy site vs. Wix staging site disagree.** Verified directly from the local legacy file (`uaecne-legacy-welcome.html`) footer, 2026-08-18: `+961-1-565-628`, `P.O. Box 11-0377`, `Riad El Solh 1107 2040`, `Beirut, Lebanon`, `secretary@uaecne.org`. This is genuinely different from the Wix staging site's footer (already in use in `Footer.tsx`): `Kobayyat Street; Sector 67; Building # 228, Jeitawi Region, Ashrafieh, Beirut, Lebanon`, `office.director@uaecne.org`, plus a second phone number (`+961-1-443547`) not present on the legacy site at all. Only one phone number roughly matches across both (`565-628`/`565628`). Not resolving which is correct — Yeghia is deciding this directly.

6. **Legacy footer utility pages are themselves unbuilt on the legacy site.** Site Map, Terms & Conditions, Credits, and Follow Us are all anchor-only stubs (`#`) in the legacy site's own footer — never real pages there either. In scope for this rebuild; will need to be built from scratch like any other missing-content page, not treated as "existing content to migrate."

7. **Legacy footer's "Contact Us" link is a self-referential bug**, pointing back to `/en/welcome` instead of `/en/contact-us`. Distinct from the real Contact Us page (linked separately near Donate), which is unaffected — noting this so the bug doesn't get mistaken for "no Contact Us page exists."

8. **Seal/logo repeated below footer content on the live Wix site — confirmed real, not built yet.** Initially flagged as unconfirmed (my own `WebFetch` check couldn't detect it, a text-extraction tool limitation, not a contradiction). Now confirmed by Yeghia via direct desktop screenshot, 2026-08-18. Not yet added to `Footer.tsx`.

9. **Donate CTA has no working destination.** Both Wix's nav Donate link and its homepage Donate banner button point to `https://www.uaecne.org/donate` — the legacy site being replaced by this rebuild, not a page within it. Confirmed still the case as of a fresh check, 2026-08-18 (matches what the Phase 1 PDF flagged back in May 2026, section 3.1: "Donate Button | Present — links to uaecne.org/donate (external)"). Could not verify whether that legacy URL even works for real visitors — same `ECONNREFUSED` as every other `uaecne.org` check this session. No real donation page/form exists within this rebuild's scope yet (not part of Steps 1–5), and per rule 7 a donation system integration needs Yeghia's explicit approval before being wired in regardless. `DonateBand.tsx`'s CTA is built as visually-present-but-non-functional (same treatment as the ՀԱՅ toggle and Instagram/X links) until a real destination is decided.

10. **⚠️ AI-GENERATED IMAGES IN CHURCH/SCHOOL FOLDERS — DO NOT USE AS REAL PHOTOGRAPHY.** Ten files across seven folders are AI-generated (filenames say so directly — "Gemini_Generated_Image_\*" or "ChatGPT Image \*"), not authentic photographs of the actual churches/schools, even though they sit in folders named after real institutions and could easily be mistaken for real documentation later:
    - `Churches/Lebanon/Arm. Evang. Emmanuel Church/Gemini_Generated_Image_2o9k972o9k972o9k.jpeg`
    - `Churches/Lebanon/Arm. Evang. Emmanuel Church/Gemini_Generated_Image_v1td91v1td91v1td.jpeg`
    - `Churches/Lebanon/Armenian Evangelical Church Ashrafieh/Gemini_Generated_Image_w21td5w21td5w21t.jpeg`
    - `Churches/Lebanon/Armenian Evangelical Church Ashrafieh/Gemini_Generated_Image_y3tyzpy3tyzpy3ty.jpeg`
    - `Churches/Lebanon/Armenian Evangelical Church Ashrafieh/ChatGPT Image Aug 16, 2026 at 03_53_15 PM.png`
    - `Churches/Lebanon/Armenian Evangelical Nor Marash Church/Gemini_Generated_Image_jxmrkwjxmrkwjxmr.jpeg`
    - `Higher Education/Haigazian University/Gemini_Generated_Image_7gawx37gawx37gaw.jpeg`
    - `Schools/Armenian Evangelical Central High School Ashrafieh/Gemini_Generated_Image_gb29hhgb29hhgb29.jpeg`
    - `Schools/Armenian Evangelical College/Gemini_Generated_Image_uszt6suszt6suszt.jpeg`
    - `Schools/Shamlian Tatigian/Gemini_Generated_Image_qq07qvqq07qvqq07.jpeg`
    Notably, for Emmanuel Church, Nor Marash Church, and Armenian Evangelical College, these AI-generated files are the *only* building-type imagery in their folders — no real exterior photo exists for those three institutions locally. Treating any of these as real photography anywhere on the site would violate rule 1.

11. **"Explore our Legacy" link (Spiritual Heritage section) has an unconfirmed destination — treated as non-functional, same pattern as the Donate CTA and Header Donate button (see Resolved).** Wix's link text was found via `WebFetch`, 2026-08-18, but its target URL was never checked (not part of that fetch's scope). `SpiritualHeritage.tsx` renders it as a bare `<span role="link" aria-disabled="true">`, no `href`, no `tabindex` — visually present, not keyboard-focusable, not a real link — until a destination is confirmed. Link copy casing corrected to "Explore our Legacy" (lowercase "our"), 2026-08-19, matching the approved mockup exactly.

12. **Home-directory git repo.** `C:\Users\Yeghia` itself is the root of a `.git` repository tracking the entire user profile (`.ssh`, `AppData`, `NTUSER.DAT`, etc.) — clearly unrelated to this project. `Website/` has its own nested `git init`, scoped only to this project; nothing here gets committed to the home-directory repo. Out of scope to fix as part of this project, but flagged as a standing note so it isn't mistaken for this project's version control later.

13. **RESOLVED — see Resolved section (Spiritual Heritage real photo).** Kept as a numbered placeholder rather than deleted-and-renumbered, so later items don't shift position across separate commits; the substantive note now lives in Resolved.

14. **Footer Western-Armenian mirror column — structure built (Unit 4), real copy still needed directly from Yeghia.** `Footer.tsx` now has a labelled, clearly-pending Western-Armenian mirror column ("Western Armenian — verified copy pending" tag). No Armenian text has been added — per rule 1/rule 2 it stays empty until Yeghia supplies verified Western (Lebanese) Armenian copy directly (never machine-translated, never Eastern Armenian). This item stays open until that copy is supplied and built in; see Resolved for the structural work.

15. **Unused assets — five superseded/orphaned photo files.** `public/sacred-legacy-building.jpeg` (superseded by `sacred-legacy-faec.jpg`), `public/ministry-churches.jpeg` (superseded by `ministry-churches-faec.jpg`), `public/ministry-youth.jpg` (superseded by `ministry-youth-camp.jpg`), `public/general-assembly-180.jpg` (the anniversary logo, superseded by the real photo `news-general-assembly.jpg`), and `public/aec-college-logo.png` (kept during the Churches build for anticipated school use — see Resolved — but the Schools index ended up sourcing a fresh copy directly from `Schools/Schools Pagge` instead, so this file is now genuinely unused) are no longer referenced anywhere in the code. Left in place rather than deleted unprompted; safe to remove in a future cleanup pass once confirmed.

16. **Publications & Media — still no photo.** Not checked this pass (Unit scope was Churches, Education, Youth, Cultural & Heritage, Social & Medical Care only). Stays on the `ArchFrame`/placeholder treatment until a real photo is sourced and verified.

17. **Anjar school (Armenian Evangelical Secondary School, Anjar) has no real building photo.** Card renders the standard photo-pending placeholder; its logo is used only for the small emblem seal, never in the photo slot.

18. **RESOLVED — see Resolved section (AEC college logo).** Kept as a numbered placeholder rather than deleted-and-renumbered, so later items don't shift position across separate commits; the substantive note now lives in Resolved and item 15 above.

19. **Nor Marash church email (`aessa68@gmail.com`) reads like a school/association address, not the church's own — kept verbatim per the sheet; confirm with the church directly.**

20. **Emmanuel Nor Amanos — no service time given in the source doc.** `churches.ts`'s "Sundays 10:00-11:15" is sourced only from the directory spreadsheet, not corroborated anywhere in the reference file (facts bar stays generic "Sunday Worship / Weekly Service," no invented clock time on the built page) — same single-sourced status as FAEC/Syriac (items 32/33). Yeghia to confirm against the church.

21. **Emmanuel Nor Amanos pastor/secretary spelling — "Svadjian" (confirmed correct by Yeghia) vs `churches.ts`'s "Sevadjian," for both the pastor (Rev. Hovhannes) and the secretary (Mrs. Ani); `churches.ts` needs an eventual update to match. The church-detail page uses "Svadjian" throughout (masthead, pastor card, leadership, and a `contactOverride.secretary` patch for the contact card) — `churches.ts` itself was not edited.**

22. **Syriac Bouchriyeh — org name "Syriac" (doc/project) vs building signage "SYRIAN EVANGELICAL CHURCH."**

23. **Syriac Bouchriyeh pastor name — "Salim" (doc) vs "Selim" (photo filename).**

24. **Syriac Bouchriyeh founding year 1966 — sourced from a building inscription, not the text doc.**

25. **Nor Marash email — reference file explicitly states it was left blank in the source, "not the church's"; stronger than item 19's loose flag, and `churches.ts`'s filled-in value (`aessa68@gmail.com`) should be treated as suspect, not authoritative. Resolved-in-render, 2026-08-20: the live church-detail page correctly omits the email entirely (see item 34) — still pending a real email from the church itself.**

26. **FAEC Beirut masthead logo — reused from the Armenian Evangelical College; plausible but not independently confirmed as an intentional/approved reuse.**

27. **FAEC Beirut pastor spelling — normalized to "Jirair Ghazarian" per the file's own note; already matches `churches.ts`, flagged as a normalization decision that happened, not a fresh discrepancy.**

28. **Anjar index card (`churches.ts`) shows its logo as the photo — wrong field mapping; out of scope this unit.**

29. **A real Anjar building photo already sits unused in `public/` that should have been used instead of the logo; out of scope this unit.**

30. **Syriac Bouchriyeh email — reference file's `syriac-evangelical-church@hotmail.com` (church's own supplied doc) conflicts with `churches.ts`'s `syriac.evan.church@gmail.com`. The reference file is authoritative for `ChurchContent`; `churches.ts`'s value is not propagated into the church-detail page and should be treated as suspect, not authoritative — same handling as the Nor Marash email (item 25).**

31. **FAEC Beirut directory data (`churches.ts`) disagrees with the Yeghia-approved reference file on two facts: neighborhood ("Clemenceau, Mexique Street" vs the reference's "Kantari, Beirut, Lebanon") and founding year ("1922-1923" vs the reference's "1922"). The church-detail page's masthead renders the reference's wording (`ChurchContent.masthead`, verbatim from `uaecne-church-faec-beirut-complete.html`), not `churches.ts`'s — `churches.ts` itself was not edited. Flagging for Yeghia to reconcile the directory data later.**

32. **FAEC Beirut service time "Sundays 10:30-11:30" is sourced only from `churches.ts`/`UAECNE_Churches_Directory.xlsx` — not corroborated anywhere in the reference file (its facts bar and contact card have no specific clock time at all). Yeghia to confirm against the church.**

33. **Syriac Bouchriyeh service time "Sundays 10:00-11:00" — same status as item 32: sourced only from `churches.ts`/the directory spreadsheet, no specific clock time anywhere in the Syriac reference file. Yeghia to confirm against the church.**

34. **`design-reference/uaecne-church-nor-marash-complete.html` has a live bug at source, confirmed by Yeghia (author of the file): its own header comment says the email was "blank in source → omitted (NOT the school's aessa68)," but the file's actual contact-card body still renders `aessa68@gmail.com` — the value the comment itself warns against. The ported church-detail page correctly omits the email (per Yeghia's ruling, trusting the comment over the buggy body here — not the same situation as the Syriac pastor-card case). The reference file itself should be corrected at some point so it stops carrying the wrong value in its body.**

35. **Pastor surname romanization conflict across two churches, likely the same person — RESOLVED for the built pages, 2026-08-20.** The Nor Marash reference spells the current pastor (2014–present) "Rev. Raffi **Messerlian**." The Anjar reference itself doesn't have a Latin-script form for its own 1998–2013 pastor at all — only Armenian, "Վեր. Րաֆֆի Մըսրլեան" (see item 39). Yeghia supplied the verified English form directly: "Rev. Raffi Messerlian," applied on the Anjar succession entry (`name`) alongside the Armenian (`nameHy`) — not a transliteration invented here, a Yeghia-verified value. Both churches now show the same spelling for what's evidently the same man.

36. **Emmanuel Nor Amanos directory data (`churches.ts`) disagrees with the reference file on address and phone.** Address: `churches.ts` has "Maggie el Hajj Street, Bouchrieh, Beirut, Lebanon / P.O.Box 80-092 Bourj Hammoud"; the reference's own "Our Location" card and masthead both say "Nor Amanos, Baouchrieh, Mount Lebanon" — no street name, no P.O. box, "Mount Lebanon" not "Beirut." Phone: `churches.ts` has one number ("01-241 636"); the reference has two ("(01) 241636 · 81 161 207," same first number, an additional line not in `churches.ts` at all). The church-detail page renders the reference's wording for both (`contactOverride.address`/`.phone`) — `churches.ts` itself was not edited. Same pattern as FAEC (item 31).

37. **Ashrafieh service time "Sundays 10:30-11:30" is sourced only from `churches.ts`/`UAECNE_Churches_Directory.xlsx`** — not corroborated anywhere in the reference file (facts bar stays generic "Sunday Worship / Weekly Service," no invented clock time on the built page). Same single-sourced status as FAEC/Syriac/Emmanuel (items 32/33/20). Yeghia to confirm against the church.

38. **Correction to item 37's original note:** Ashrafieh's `churches.ts` record was described as matching its reference "exactly" on address — more precisely: reference and `churches.ts` **agree** on phone, email, secretary, and the address's main line (verbatim, "Rue Younes Gebeili, Geitawi, Beirut, Lebanon"). `churches.ts` additionally carries a second address line, "P.O.Box 80758, Bourj Hammoud," that the reference simply omits (not a conflicting value — the reference's own contact card just doesn't include it). Per Yeghia's ruling, this is a superset, not a conflict: the reference wins on genuine disagreements (see FAEC, item 31), but doesn't override real supplementary detail `churches.ts` carries and the reference doesn't contradict. The P.O. box stays on the live page, sourced from `churches.ts` as before — flagged here only so its `churches.ts`-only provenance is traceable.

39. **Anjar succession list — 7 of 9 pastors render in verified Armenian script only, by deliberate decision, not a gap.** `uaecne-church-anjar-complete.html`'s entire "Pastors of the Church" list (plus its intro line) is authored in Armenian only, with no English form given anywhere in the document for 7 historical entries (1939–1999). Per Yeghia's ruling, these render their real, verified Armenian text (`SuccessionEntry.nameHy`, `name: null`) in the site's Armenian typeface (Noto Serif Armenian, newly wired into `layout.tsx`/`tokens.css` for this — the first verified Armenian content built into the live site) rather than an invented English transliteration. Will flip to English automatically once Yeghia supplies a verified English roster for these 7 names — no code change needed then, just data.

40. **Anjar directory data (`churches.ts`) address is a superset, not a conflict — same pattern as Ashrafieh (item 38).** Reference's "Anjar, Bekaa Valley, Lebanon" is a trivial paraphrase of `churches.ts`'s "Anjar, Bekaa, Lebanon"; `churches.ts` additionally carries a real P.O. box ("P.O.Box 66, Zahle, Bekaa") the reference omits. Kept on the page from `churches.ts`, no override. Phone/email/secretary all agree exactly between the two sources.

41. **Anjar succession — Rev. Nersès Balabanian's tenure (1990–1999) and Rev. Raffi Messerlian's (1998–2013) overlap by one year in the reference's own dates.** Not a duplicate row (each of the 9 entries appears exactly once in the source) — just an unexplained 1-year overlap in the reference's own succession years. Rendered verbatim as given; not resolved or smoothed over.

42. **Anjar reference HTML (`uaecne-church-anjar-complete.html`) misspells the assistant pastor's surname — "Mncherian" (both occurrences: About text, leadership card) — vs the source doc's "Mencherian"** (`Churches/Lebanon/Armenian Evangelical Church , Anjar/The Armenian Evangelical Church of Anjar 2026 (1).docx`, extracted and verified directly: "Pastor Asadour **Mencherian** joined our church as assistant pastor"). Per Yeghia's ruling, the source doc wins — same category as the Nor Marash email bug (item 34): the reference HTML itself should be corrected later. The built Anjar page uses "Mencherian" throughout.

43. **AnniversaryBand uses four ad-hoc hex values (`#fcf8ee`, `#f2e7d0`, `#a8863f`, `#fff`/`#f3e9d2`) for its decorative gradient/inner-disc — no matching design token existed.** Reconcile if the palette is ever refined.

44. **`churches.ts`'s Anjar `photo` field (`/church-armenian-evangelical-church-anjar-logo.png`, the logo, not a building photo — see items 28/29) is now genuinely redundant.** The churches-index card no longer reads it (Wave 1 fix: cards derive photo/emblem from `churchContent[slug]` when a detail page exists, falling back to `churches.ts` only for the 19 churches without one yet) — Anjar has a `ChurchContent` entry, so its card now shows the real hero photo regardless of this field's value. `churches.ts` itself was not edited this pass (out of scope); this field could be corrected or left alone in a later data-cleanup unit, since nothing in the live site reads it anymore for Anjar specifically.

45. **AEC "Vice-Chair of the Council" — no name found anywhere in the source `.odt`.** Renders "Name pending" (arched placeholder, no photo) in both the About-adjacent Leadership grid slot per the template rule — never guessed. Confirm with the College directly.

46. **AEC's `OPEN_QUESTIONS` item 10 is now stale.** Item 10 (see AI-generated-images list) implied no real local building photo existed for AEC at the time it was written; a real one does (`Schools/Armenian Evangelical College/Armenian Evangelical College.jpeg`, MD5-confirmed identical to the already-tracked `public/school-armenian-evangelical-college.jpeg` used on both the index and the new detail page). Superseded, not deleted — kept for history.

47. **AEC has no independently-verified enrollment/student-count figure.** The source `.odt` gives founding/reorganization dates and named leadership but no total student number — the facts bar uses "Secondary / Grade Levels" instead of a count, to avoid inventing one.

48. **Shamlian-Tatikian principal name/photo mismatch — not resolved, rendered as a real, flagged gap.** The source doc (`Arm. Evang. Sgamlian School - History.docx`) names the current principal as "Mrs. Kayane Tunberian." The only two current-day staff photos on file are labeled "Mrs. Tamar — Secretary" and "Mrs. Kayane Messerian" — neither matches "Tunberian." The built page shows the verified name with an arched photo-pending placeholder (both in the About principal card and the Leadership grid); the two candidate photos were NOT used anywhere. Confirm with the school directly.

49. **Shamlian-Tatikian has no verified contact email, phone, or address anywhere in its source doc.** The "Our Location + Get in Touch" section and the "Make an Inquiry" section are both omitted entirely from its page (conditional omission, not a placeholder) — a real gap, not an oversight. Needs real contact info sourced before either section can be added.

50. **Shamlian-Tatikian has no Mission-statement or Academic-Heritage-style era content in its source doc.** Its "About the School" section carries the real historical narrative instead (the doc's own "Our History"/"A Brief History" text, verbatim); the Mission and Academic Heritage sections are omitted, not filled with invented text.

51. **Shamlian-Tatikian's closing CTA band has no source text at all (no design mockup exists for this school specifically).** Written in the same house-style editorial convention already used for every church CTA in `churchContent.ts` — anchored only to verified facts (1934 founding, Bourj Hammoud, the Union) — not a verbatim quote from any reference document. Flagging so it can be revised or replaced once real closing copy is supplied.

52. **An AI-generated file exists in the Shamlian-Tatikian source folder** (`Gemini_Generated_Image_qq07qvqq07qvqq07.jpeg`) — identified by filename pattern per item 10's standing rule, not used anywhere.

53. **AESSA founding-year conflict — not resolved, rendered as AESSA's own stated year.** AESSA's own source doc and its own approved design mockup both say "Established: 1942." The already-committed Anjar CHURCH page states the church was "founded in 1941, one year after the Armenian Evangelical School of Anjar" — implying ~1940, two years earlier. The AESSA page uses 1942 (its own primary source); the church page was not touched or reconciled. Yeghia to resolve with the source directly.

54. **Rev. Hagop Akbasharian's tenure-start date differs by one year between the two pages he appears on** — Anjar church: Pastor since 2013–present; AESSA: Director since 2014–present. Same person (same photo file, MD5-identical), reported as-is, not smoothed over.

55. **RESOLVED (2026-08-21) — director/pastor name provenance, reconciled across both pages.** AESSA Directors Archive and the Anjar church's Pastors succession share four names with overlapping year ranges (Sarmazian, Balabanian, Messerlian, Akbasharian) — the same parish clergy historically holding both the church-pastor and school-director roles concurrently. Yeghia has now directly verified the English spellings for all 7 AESSA directors (previously 4 were Armenian-only, flagged "to be confirmed" in the school's own approved mockup). Sarmazian and Balabanian's confirmed English spellings ("Rev. Hovhannes Sarmazian," "Rev. Nerses Balabanian") were also applied to the Anjar CHURCH's own succession entries for the same two people (`churchContent.ts`), which previously rendered them Armenian-only — a deliberate, Yeghia-approved data correction, not a guess. Janbazian (AESSA director #2) does not appear anywhere in the church's succession list, so no church-side reconciliation was needed for him. One person, one spelling, consistent across both pages now.

56. **BUILT (template-expansion unit, 2026-08-21).** AESSA's four held-back sections are now live, matching `design-reference/uaecne-school-anjar-template.html` exactly: Academic Excellence & Innovation (reuses `SchoolAcademics`/`academicHeritage`, 4 theme-label pill rows — decorative side-crest intentionally skipped, standard eyebrow+heading kept instead of the reference's bare `<h3>`); Our Mission & Philosophy (new `missionValues` field + `SchoolMissionValues` component — intro quote + 4 icon-less red-gradient value cards; deliberately not merged into the existing icon-arch `SchoolMission`, which stays AEC-only and untouched); Inclusive Support Services (new `supportServices` + `SchoolSupportServices` — Healing Harbour uses its real logo file, Boarding Shelter/PEP use generic icons, no logo exists for either); Signature Programs (new `signaturePrograms` + `SchoolSignaturePrograms` — WeNEEDle uses its real logo file, Seeds of Hope/Student Life & Clubs use a generic leaf mark); Faith & Community (new `faithCommunity` + `SchoolFaithCommunity` — 4 cards including "Our Team," all 4 photo slots on "photo pending" since no real photo exists yet for any of the 4 scenes). Directors Archive (item 56's original 4th item) was already built in the prior unit. All 4 new fields are nullable and null for AEC/Shamlian-Tatikian — both re-screenshotted (desktop + mobile) and confirmed unaffected.

57. **Template amendment A (heroPhoto → nullable) and B (principal-slot role label) — B required no code change.** `principalCard.role` and each `SchoolLeaderEntry.role` were already free-form per-school strings in the committed type/components (not hardcoded "Principal" anywhere) — AESSA's "School Director" label just uses the existing field. Only amendment A (nullable `heroPhoto`, with `SchoolTopBlock` falling back to the schools-index-card "photo pending" visual language) was an actual code change. Verified AEC and Shamlian-Tatikian render unaffected (re-screenshotted both, desktop + mobile, byte-for-byte identical to the pre-amendment build).

58. **`schools/page.tsx`'s `cardPhoto`/`cardEmblem` derivation fixed alongside amendment A.** The original Wave-1-style helpers used `schoolContent[slug]?.heroPhoto ?? fallback`, which would have incorrectly fallen through to `schools.ts`'s own photo field if a `SchoolContent` entry ever set `heroPhoto` explicitly to `null` (as AESSA now does) — `??` can't distinguish "verified: no photo" from "no entry at all." Fixed to check entry existence directly. Harmless before this unit (no school had a null `heroPhoto` yet); real and immediate now.

59. **Hamegh is not an AESSA-page section.** Confirmed by reading the AESSA docx/reference in full: "Hamegh" never appears anywhere in AESSA's own content (Signature Programs lists only Seeds of Hope, WeNEEDle, Student Life & Clubs). Its logo file (`Hamegh LOGO 1.avif`, real, confirmed) sits in the same shared Anjar source folder only because the church's Hamegh project and AESSA are both Anjar-based institutions — not used on the AESSA page. The Anjar church's already-committed Hamegh content (`SpecialProjectBand`, commit `3c25338`) was not touched.

60. **PENDING REAL EMAIL — AESSA "Make an Inquiry" intentionally still omitted.** `uaecne-school-anjar-FULL.html` (the full-page reference) shows the section, but its own button has no real destination (`href="#"`), and the same file's own Contact card admits "Email: Pending" — there is no real school email to build a working `mailto:` link from. Building the section anyway would mean either a fake email or a dead-end button, neither acceptable per PROJECT_BRIEF rule 7 and the standing "never invent" discipline. Same treatment as Shamlian-Tatikian. On 2026-08-21 Yeghia said a real email was ready to supply, but the actual address was never given (a placeholder token arrived instead of a real value) — `inquiry: null` and the Get in Touch "Email" row both stay "Pending" until the real address is supplied directly. Build the Inquiry section and update the Email row together in one pass once it arrives.

61. **RESOLVED — "Boarding Home" confirmed by Yeghia (2026-08-21), matches the real logo.** The card was renamed based on the school's own real logo file, which prints "ARMENIAN EVANGELICAL BOARDING HOME – ANJAR, LEBANON" — this now stands as the confirmed institution name over the older mockup/docx wording ("Boarding Shelter").

62. **Higher Education index live (2026-08-21).** New `/higher-education` section under Ministries — index + 3 detail pages (Education Council, Haigazian University, Near East School of Theology), route + nav wired. See items 63–65 for the specific gaps carried over from this unit.

63. **Education Council has no logo/seal file anywhere in its source folder.** The hero and index card both use a generic graduation-cap seal/icon treatment instead — never invented. If a real Council seal exists and is supplied later, swap it into `educationCouncilContent` (currently `logo`-less by design, not a nullable field left unset).

64. **Haigazian enrollment figure omitted — conflicting sources, not resolved.** No single verified enrollment number is used anywhere on the Haigazian page; the approved mockup itself carries none, and no other verified figure was supplied this unit. Omitted rather than guessed. Confirm a real figure with Yeghia before adding one.

65. **NEST leadership photo tone — Dr. Rima Nasrallah van Saane's photo reads noticeably darker/more dramatic than Dr. Martin Accad's in the small circular "Office of the President" card.** Not a cropping bug — the source photo (`Rima Nasrallah.jpg`) is a conference-stage shot against a black backdrop, while Accad's is a bright candid photo; both are real, respectful, unedited photos of each person, just different in original lighting/setting. Flagging for Yeghia's review in case a different (brighter) photo of her is preferred for this slot.

66. **Outreach section live (2026-08-21).** New `/outreach` section — index + 3 detail pages (Camps/KCHAG, Youth Work/CE Union, Social Action Committee), nested under Ministries in the nav (initially built as a top-level item per the original instruction, corrected mid-build to sit under Ministries alongside Churches/Schools/Higher Education). See items 67–70 for gaps carried over from this unit.

67. **Social Action Committee naming — deliberate standardization, not a content error.** The source doc ("SAC.docx") says "The Social Work Center of the Union..."; a second source doc ("SAC 2- Lena.docx") uses "AESAC" in its own heading. Yeghia ruled to standardize on "Social Action Committee" throughout the page, including correcting the About paragraph's own wording to match the page's title — approved 2026-08-21.

68. **Camps gallery, Youth Work group hero, and all Social Action Committee photos (hero + 2 program-pillar rows + 3 children's-program cards) are photo-pending.** No real files exist for any of these yet — every slot renders the shared "photo pending" language, never invented. Real photos to be supplied by Yeghia later.

69. **No logo/seal exists for KCHAG, the CE Union, or the Social Action Committee.** Confirmed by a full listing of the Outreach source folder — none of the three has a logo file anywhere. The masthead on each detail page has no seal graphic (unlike Higher Education's institutions, all of which have real seals); revisit if a logo is supplied later.

70. **Basmajian photo reused, not duplicated.** `Outreach/Youth work/Pastor Datev Basmajian-Member ot large - Lebanon .JPG` is MD5-identical to the already-tracked `public/central-committee-datev-basmajian.jpg` (from the earlier Central Committee build) — the Youth Work director card points at that existing path rather than a new copy.

71. **History page's 4 thematic-pillar archival photos (Education, Compassion, Social Outreach, Children & Youth) are all photo-pending.** No archival photos were supplied for any of the four pillars — each renders the shared arched "Archival photo pending" placeholder, never invented. Real photos to be supplied by Yeghia later.

73. **Bethel Armenian Evangelical Church of Aleppo — all of the following are pending Union approval, per the approved mockup's own placeholder language, never fabricated:**
    - **Phone and email** render as "Pending" even though `churches.ts`'s directory row has values for both (`(963-21) 4642-450 / 4467-528`, `bethelchurch31923@gmail.com`) — the approved mockup explicitly marks both "Placeholder — pending," which wins per rule 1 (verified reference over directory guess). Flagging the directory/mockup conflict itself, not just the pending state.
    - **Street address** — only the district-level location ("Jabriye District, Aleppo, Syria") is confirmed; the exact street address is pending per the mockup's own note.
    - **Worship service time** — mockup gives only "Sunday · pending," no confirmed time.
    - **16 of 17 pastors'-succession name spellings are best-effort English transliterations, pending Union confirmation** (Rev. Misak Manoukian, Rev. Hovhannes Abkarian, Rev. Nerses Khachadourian, Rev. Dikran Andreasian, Rev. Vahan Bedikian, Rev. Yesayi Sarmazian, Rev. Bargev Abardian, Rev. Manase Shnorhokian, Rev. Hovhannes Sarmazian, Rev. Ardashes Kerpabian, Rev. Bargev Orjanian, Rev. Hanna Sarmazian, Mr. Melkon Melkonian) — transliterated from the mockup's own Western Armenian script since no English form exists anywhere in the source docs for these. 5 names (Garabed Ketenjian, Nerses Sarian, Yeghia Kassouni, Aharon Shirajian, Haroutune Selimian) already had a confirmed English spelling directly from the mockup's own History prose, so those are not flagged as pending.
    - **No distinct logo/seal file exists** for Bethel — the masthead renders without a logo circle (same treatment as any other church with none), rather than reusing the building photo as a fake emblem.
    - **`churches.ts`'s directory name ("Armenian Evangelical Bethel Church, Aleppo") differs slightly from the approved mockup's h1 ("... Bethel Church of Aleppo")** — `ChurchTopBlock` renders `church.name` from the directory (never invented/reformatted per that file's own sourcing rule), so the live page shows the directory's phrasing. Flagging the discrepancy rather than silently editing either source.
    - Photographer watermarks ("Studio Venus," "Ward Media Group") are visible in several of the real source photos (hero, pastor portrait) — used as-is, not cropped or edited, since altering a supplied photo wasn't authorized.

74. **Site-wide: Armenian-language church pages deferred to a later phase — English-only shipped first for the Syria unit.** Applies to all 9 Syria churches (Aleppo ×4, Damascus ×1, Kessab ×4): every mockup's Armenian content (masthead subtitle lines, in Bethel's case the full pastors-succession list) is either dropped or transliterated to English per Yeghia's explicit instruction; no `*Hy` fields are populated for any of them yet.

75. **Emmanuel Armenian Evangelical Church of Aleppo — all of the following are pending Union approval, per the approved mockup's own placeholder language, never fabricated:**
    - **Phone, email, and street address** all render as "Pending" — the mockup gives none of the three a real value at all (unlike Bethel, where churches.ts had values the mockup overrode; here churches.ts's own phone/email are simply not used, matching the mockup's own "Pending" treatment).
    - **Betty Hatem and Tamar Kazanjian-Keoshgerian's leadership photos are photo-pending** — no photo file exists for either person; both render the arched "Photo pending" placeholder with their real names, per the mockup.
    - **No distinct logo/seal file exists** for Emmanuel — the mockup's own masthead circle is a generic cross icon (not a real institutional emblem), so the masthead renders without a logo, same as Bethel.
    - **`churches.ts`'s directory name ("Armenian Protestant Emmanuel Church, Aleppo") differs from the approved mockup's h1 ("Armenian Evangelical Emmanuel Church of Aleppo")** — same category of directory/mockup naming discrepancy as Bethel (see item 73); `church.name` renders from the directory, unchanged.
    - Rev. Dr. Haroutune Selimian's real photo is **reused from Bethel's own page** (`/church-bethel-aleppo-pastor-selimian.jpg`), not duplicated — he is the same real person, legitimately serving as Emmanuel's Honorary (non-resident) Pastor per the mockup's own text, so this reuse is accurate rather than misleading (distinct from the Kessab case flagged separately, where reusing one pastor's photo across churches he does not serve would be a factual error — see the Kessab units when built).

76. **Two bugs found in already-committed shared church primitives, caught while building Emmanuel and fixed for both Bethel and Emmanuel:**
    - `LeadershipGrid` hardcoded its eyebrow as "The Ministry"; both Bethel's and Emmanuel's mockups say "The Team." Made the eyebrow/heading optional overrides (default unchanged for every other church) and set `leadershipEyebrow: "The Team"` on both entries.
    - `ChurchTopBlock`'s masthead meta line hardcoded the prefix word "Established"; both Bethel's and Emmanuel's mockups say "Founded" instead, and Emmanuel additionally needs a second dated value ("Present church 1925") the component had no way to render at all. Added an optional `establishedLabel` override and an optional `secondDate` field (both additive, every other church's rendering unaffected); set `establishedLabel: "Founded"` on Bethel's entry retroactively.

77. **Martyrs' Armenian Evangelical Church of Aleppo — all of the following are pending Union approval, per the approved mockup's own placeholder language, never fabricated:**
    - **Phone, email, and street address** all render as "Pending" — matches the mockup's own placeholder treatment.
    - **Secretary honorific — "Mrs." used, flagged pending.** The English source doc calls her "Mrs. Sarin Seraydarian-Aghayegian" (matching Տիկ., the Armenian honorific for a married woman); a separate Armenian office-list gloss elsewhere said "Miss" for the same person. Used "Mrs." per the doc and the mockup's own text, but the discrepancy itself is flagged, not silently resolved.
    - **No distinct logo/seal file exists** — masthead renders without a logo, same as Bethel/Emmanuel (mockup's own circle is a generic cross icon, not a real emblem).
    - **`churches.ts`'s directory name ("Armenian Evangelical Martyrs' Church, Aleppo") differs in word order from the approved mockup's h1 ("Martyrs' Armenian Evangelical Church of Aleppo")** — same category of directory/mockup naming discrepancy as Bethel/Emmanuel; `church.name` renders from the directory, unchanged.
    - **Gallery treatment — deliberately the plain, non-lightbox `ChurchGallery`, per Yeghia's explicit instruction.** Flagging for the record: the mockup's own `<script>` does implement click-to-zoom open/close/prev-next on its 2 photos (just without scroll arrows, since there are only 2 images) — technically a lightbox, just not the scrollable-row variant `ChurchGalleryLightbox` was built for. Built as instructed (plain grid, no zoom) rather than silently adding a third gallery variant.

78. **⚠️ SIGNIFICANT BUG, found and fixed during Syriac's build, affecting Bethel/Emmanuel/Martyrs' already-committed rendering:** `[slug]/page.tsx` called `<ChurchAbout about={content.about} pastorCard={content.pastorCard} />` — never forwarding `content.about.dropcap`/`pullQuote`/`vacancyNote` into `ChurchAbout`'s own separate top-level props of the same names. TypeScript's structural typing allowed this silently (the `about` object literally carries those keys, but the component never read them from there). Net effect on all 3 already-committed Aleppo churches: **the About section's first paragraph was rendering WITH a drop-cap** (the component's own default, `dropcap = true`, applying regardless of each church's intended `dropcap: false`) **and each church's pull-quote line was NEVER rendering at all** (the `pullQuote` prop was always `undefined`). Fixed by explicitly passing `dropcap={content.about.dropcap}` / `pullQuote={content.about.pullQuote}` / `vacancyNote={content.about.vacancyNote}` from `content.about` at the call site. Verified by screenshot: all 3 churches now correctly show no drop-cap and a visible pull-quote line, matching their mockups. This fix is bundled into Syriac's commit (same as the two smaller label fixes in item 76) since it's a shared `page.tsx`/type change, not something that can be split out.

79. **Syriac Evangelical Church of Aleppo — all of the following are pending Union approval; this entire page was translated from an Armenian-only source, so every name below is a placeholder, not a confirmed spelling:**
    - **Pastor Jirjis Shemes, Father Youhanna Toro, Pastor Youssef Osta Jabbour** (historic pastors archive) and **Miss Mary Hakko** (Vice-Chairwoman) — all English spellings pending Union confirmation, per the mockup's own note ("English name spellings pending confirmation" / "English spelling pending your confirmation").
    - **Phone and email render as "Pending"** even though `churches.ts` has values for both (`(963-21) 2211-782 / 2234-020`, `Maryhakko18@gmail.com`) — the mockup explicitly marks both pending, which wins per rule 1 (same precedent as Bethel).
    - **Address kept as the mockup's own "Aleppo, Syria"** (not churches.ts's more specific "Assyrian Quarter, Aleppo, Syria") so it doesn't contradict the mockup's own "district and street — pending" note. Flagging the directory's more-specific value here in case it actually resolves the "district" half of that pending note.
    - **No distinct logo/seal file exists** — masthead renders without a logo, same as the other 3 Aleppo churches.
    - **Pulpit vacant** — no `pastorCard`, single leadership card only (Mary Hakko), no succession/current-pastor highlighting.
    - **2 of the 4 gallery photos are church-approved AI renders**, not photographs — "Congregation at worship" and "Children's gathering." Provenance: both exist as separate real files in the source folder (not just embedded in the mockup), each already carrying a visible "Made with AI" watermark baked into the image itself by whatever tool generated them; the built page additionally overlays the mockup's own "Church-approved render" badge on the gallery thumbnail. Kept as directed, not swapped for anything else.

80. **Damascus Armenian Evangelical Church — all of the following are pending Union approval, per the approved mockup's own placeholder language, never fabricated:**
    - **Phone, email, district, and street address** all render as "Pending" — the mockup gives only the city ("Damascus, Syria"), nothing more specific.
    - **No current pastor** — the mockup has no pastor card, no leadership grid, and no programs module; none of the three is built. `churches.ts`'s new directory row leaves `pastor` blank rather than fabricating "Vacant" (unlike Emmanuel/Syriac, whose original directory rows already said so) — Damascus never had a prior directory row to draw that word from.
    - **Rev. Garabed Hasessian and Rev. Nerses Sarian** (early-pastors archive) — English spellings pending Union confirmation, per the mockup's own note.
    - **No distinct logo/seal file exists** — masthead renders without a logo, same treatment as several other Syria churches; the mockup's own masthead circle is a generic cross icon, not a real emblem.
    - **No Facebook link** — the mockup has none; none added.
    - **Congregation size ("about fifteen Evangelical families") and the "thirteen resident pastors, 1930–2010" count** are both verbatim from the mockup's own text — not independently corroborated against a separate source document.

81. **Karaduran/Kaladouran (Kessab) — all of the following are pending Union approval, per the approved mockup's own placeholder language, never fabricated:**
    - **Spelling conflict — mockup says "Karaduran," `churches.ts`'s existing directory row (B-SY-09) says "Kaladouran."** Same village, two English transliterations. `ChurchTopBlock`'s `<h1>` renders `church.name` from the directory ("Kaladouran," never overridden by this file), so the live page's heading uses the directory spelling while the masthead's location line and body prose use the mockup's own "Karaduran" — same precedent as Bethel's directory/mockup name mismatch (item 73): flagging rather than silently editing either source.
    - **Phone and street** render as "Pending" per the mockup's own note, even though `churches.ts` has a phone value (`(963-41) 710-917`) — that number is the shared visiting pastor's line for all 4 Kessab churches, not specific to this inactive one; the mockup's pending language wins, same precedent as Bethel (rule 1).
    - **Email suppressed entirely** (`contactOverride.email: null`) — the mockup's own "Get in Touch" card has no email row at all; `churches.ts`'s email is that same shared pastor's address, not shown as if it belonged to this specific inactive church.
    - **Founding year conflict** — `churches.ts`'s `estYear` says "1855 (?)" (itself already uncertain); the mockup gives a confident "Founded 1873 · Evangelical witness since 1860," used for the page. `churches.ts` untouched.
    - **No distinct logo/seal file exists** — masthead renders without a logo, same treatment as several other Syria churches.
    - **No resident pastor, ever** — the congregation could never sustain one (per the mockup's own history text); worship has been held at Holy Trinity, Kessab since 2014. No pastor card, leadership grid, or programs module built — not merely empty, structurally absent from this page regardless of what `churches.ts`'s own `pastor` field says (that field is read only by the directory-only fallback route, never by this church's full-content render).

82. **Ekizolouk/Ekizolukh (Kessab) — all of the following are pending Union approval, per the approved mockup's own placeholder language, never fabricated:**
    - **Two naming gaps — mockup says "Ekizolukh," `churches.ts`'s existing directory row (B-SY-07) says "Ekizolouk"** (same spelling-conflict pattern as Karaduran/Kaladouran, item 81) **and the mockup's own name adds "Emmanuel"** ("Armenian Evangelical Emmanuel Church of Ekizolukh") that the directory's name ("Armenian Evangelical Church of Ekizolouk") omits entirely. `church.name` (the shorter directory form) still renders the `<h1>`, never overridden by this file — same precedent as Bethel.
    - **Founding year conflict** — `churches.ts`'s `estYear` says "1882"; the mockup gives "Construction begun 1911 · completed 1955," used for the page. `churches.ts` untouched.
    - **Phone and street** render as "Pending" per the mockup's own note, even though `churches.ts` has a phone value (`(963-41) 710-063`) — the same shared visiting pastor's line as Karaduran's, not specific to this inactive church; the mockup's pending language wins (rule 1).
    - **Email suppressed entirely** (`contactOverride.email: null`) — same reasoning as Karaduran: the mockup's "Get in Touch" card has no email row, and the directory's email is that same shared pastor's address.
    - **No distinct logo/seal file exists** — masthead renders without a logo, same treatment as several other Syria churches.
    - **No resident pastor since February 2020** — per the mockup's own "Church Today" callout. No pastor card, leadership grid, or programs module built, regardless of what `churches.ts`'s own `pastor` field says (read only by the directory-only fallback route, never by this church's full-content render). Unlike Karaduran, this mockup never names a specific church worship has moved to, so no "Worship Now Held At" facts-bar cell was invented.
    - **A 3rd photo file in the source folder (`IMG-20200530-WA0014 (1).jpg`) is a misfiled duplicate of Karaduran's interior photo** (MD5-identical to `public/church-karaduran-kessab-interior.jpg`), not a real Ekizolukh photo — not used. The 2 real photos actually used were matched to the mockup's own embedded images by exact pixel dimensions (1008×756 / 756×1008) plus visual inspection, not MD5 (same lesson as every Syria church so far).

## Resolved

- **HQ contact info (address, phone, email)** — confirmed real by Yeghia directly, 2026-08-18. Not sourced from any audit document (none was available to trace) — Yeghia confirmed firsthand that the staging site's footer contact details are correct as pulled via WebFetch. See `Footer.tsx`.
- **Footer copyright text** — "All Sacred Rights Reserved" (as it appears live on staging) confirmed by Yeghia, 2026-08-18, to be a copy error; corrected to "All Rights Reserved" in `Footer.tsx`.
- **Site-wide seal logo** — `uaecne-symbol.svg` (UNION root) confirmed by Yeghia as the site-wide seal; copied byte-identical to `Website/public/logo.svg`.
- **Nav top-level structure** — six top-level items provided by Yeghia, sourced from a screenshot of the staging site taken earlier in this session (not a fresh check at the time). Independently re-verified via Claude's own WebFetch of the same staging site, 2026-08-18 — matched exactly, plus additional submenu detail.
- **Full org name** — "Union of the Armenian Evangelical Churches in the Near East," provided by Yeghia directly, 2026-08-18, correcting an initial draft title. Independently matches the letterhead on `UAECNE_Phase1_Discovery_Content_Audit_Design_Direction.pdf`, page 1.
- **Heading color** — default h1–h4 color confirmed as red (`#8B0000`), not brown, by Yeghia, 2026-08-18.
- **Facebook/YouTube social URLs** — real, specific org URLs verified via WebFetch of the staging site footer, 2026-08-18. Instagram/X links on the same source are themselves generic placeholders (not org-specific), so both render as visually-present-but-inactive rather than real links.
- **Armenian HQ address text** — `Քոպայէթ փողոց, Սեկտոր 67, Շէնք թիւ 228` / `Ժէյթաուի շրջան, Աշրաֆիէ` / `Պէյրութ, Լիբանան`. Provided directly by Yeghia via chat paste, 2026-08-18 — matches the earlier `WebFetch`-transcribed version word-for-word (that version was flagged low-confidence at the time; this direct paste supersedes it as the reliable source). Not yet added to `Footer.tsx` — pending confirmation on whether/how to display it bilingually.
- **Hero copy, ticker copy, and homepage stats numbers** — all sourced from the Wix staging site (`yboghossian1.wixsite.com/uaecne-1`), 2026-08-18, via `WebFetch`, per the Content & Design Strategy sourcing waterfall (step 2, Wix current text). Checked and ruled out first: local UNION folder (keyword grep across the tree, plus full `pdftotext` extraction of both "UAECNE Website Proposal" PDFs at the UNION root — neither contains this copy, only generic "heritage" language and the outdated "170-year" figure) and the legacy site (`uaecne-legacy-welcome.html` — zero occurrences of "1846," "180 years," "unity," "enduring," or "heritage" anywhere). Cross-verified: "9 Countries" matches the independently-built site-scope inventory exactly; "180"/"1846" match the already-confirmed heritage facts; the hero headline nearly matches the brief's own placeholder text. Values: hero headline "Faith, Unity & Enduring Service," subtext "The Union of the Armenian Evangelical Churches in the Near East"; ticker "ROOTED IN FAITH SINCE 1846 • HEADQUARTERED IN BEIRUT • SERVING THE NEAR EAST • 180 YEARS OF HERITAGE • GLORY TO GOD ALONE • UNION OF THE ARMENIAN EVANGELICAL CHURCHES IN THE NEAR EAST"; stats 180 Years of Heritage / 30+ Churches / 9 Countries Served. Built into `Hero.tsx`, `TickerBand.tsx`, `StatsRow.tsx` — decided to drop the fourth stat, "12+ Institutions," as the least-verified value with no clean mapping to the site-scope inventory (Yeghia, 2026-08-18); `StatsRow.tsx` uses only the three.
- **Ministries grid source — Wix's "Our Enduring Ministries" 6-card section, not the legacy 8-category structural list.** Confirmed by Yeghia via direct tool selection, 2026-08-18 (a follow-up text message asking for the 8-category list was a self-identified mix-up, disregarded). Distinction: the legacy/site-scope 8 categories (Churches, Schools, Higher Education, Outreach, Health Care, Publications, Cultural, Countries) remain the authority for overall page *scope* and the eventual `Nav.tsx` Ministries dropdown (see item 3) — but the homepage grid is a separate, already-designed marketing surface with its own real copy on Wix, which takes priority per the sourcing waterfall's "Wix current text" step for this specific component. The 6 cards are a curated consolidation (e.g. "Education & Schools" folds Schools + Higher Education together; "Countries" isn't represented, since it isn't a ministry), not a 1:1 relabeling of the 8 categories.
- **News section content** — real item found, not a stub. Wix homepage's "LATEST UPDATES" section, `WebFetch`, 2026-08-18: headline "GENERAL ASSEMBLY," dated June 2026, excerpt "Held on June 21–22, 2026, in Beirut, the 79th General Assembly of the UAECNE marked the church's 180th anniversary under the theme 'Freedom of Conscience and Responsible Faith.'" Cross-verified against local UNION folder: `Recourses/General Assembly/Genereal Assembly 180 Ann. Logo.jpg` exists (matching the 180th-anniversary theme) plus a text file pointing to a Google Drive photo album (external, not fetchable from here — the logo image is used as the local asset instead; the "group photo" mentioned isn't locally downloaded). Legacy site's `/en/news` page not checked — no local saved copy exists (only the homepage was saved) and it's unreachable live (same `ECONNREFUSED` as before). Not a blocker: Wix's content is real, current, and cross-verified, satisfying the waterfall without needing the legacy fallback.
- **Donate band copy** — heading "Support our Mission," subtext "Your dedication and generosity empower the UAECNE to continue its vital work in spiritual growth, education, and compassionate outreach across the Near East. Join us in sustaining a heritage of faith since 1846," button label "DONATE NOW." Sourced from Wix, `WebFetch`, 2026-08-18. Subtext independently cross-verifies the 1846 founding year.
- **Stats row expanded to 4 columns, restoring "12+ Institutions."** Decided by Yeghia, 2026-08-18 — the verification gap that originally justified dropping it is closed (confirmed live/real via direct desktop screenshot). `StatsRow.tsx` data array and `StatsRow.module.css` `grid-template-columns`/stagger rules updated accordingly.
- **Header "Log In" feature — decided NOT to build.** Per project history, an unintentional Wix members-area default rather than deliberate design. Yeghia did not object to the recommendation, 2026-08-18 — treated as settled, no code change needed.
- **"A Sacred Legacy Since 1846" section — built and wired in.** New homepage section, not in the original six. Real copy sourced via `WebFetch`, 2026-08-18: paragraph text plus a Psalm 18:2 quote, used verbatim, no paraphrasing. Decided by Yeghia to add before Step 2 is done; placement confirmed via desktop screenshot (between StatsRow and MinistriesGrid). Built as `SacredLegacy.tsx`/`.module.css`, wired into `page.tsx`.
- **"Our Spiritual Heritage" section — built and wired in.** New homepage section, not in the original six. Real copy sourced via `WebFetch`, 2026-08-18: paragraph text plus a Matthew 18:20 quote, used verbatim. Placement confirmed via desktop screenshot (between News and DonateBand). Built as `SpiritualHeritage.tsx`/`.module.css`, wired into `page.tsx`. Its "Explore Our Legacy" link has an unconfirmed destination — see Unresolved item 11.
- **Header "DONATE" button — built.** Decided by Yeghia, 2026-08-18: build it with non-functional treatment, same pattern as `DonateBand.tsx`'s CTA (bare `<span role="button" aria-disabled="true">`, no `href`). Built into `Header.tsx`/`Header.module.css`, hidden below 768px (a judgment call, not explicitly requested — flagged for review).
- **News excerpt updated to the fuller Wix text.** The originally-used excerpt was truncated relative to the real content; the fuller version (voting members, committee elections, memorial, Holy Communion service) is now in `News.tsx`, plus a "Read more" link with the same non-functional treatment as other unconfirmed-destination links this session (unconfirmed destination, not checked as part of the original fetch).
- **MinistriesGrid built with mixed real-photo/placeholder treatment.** Per the sourcing waterfall check (local UNION folder, by category): real photos used for Churches & Missions (`ministry-churches.jpeg`, from `Armenian Evangelical Church Anjar Front - image.jpeg`), Youth & Camp Ministries (`ministry-youth.jpg`, from `Photos/CE ministry photos/WCE/`), and Education & Schools (`ministry-education.jpg`, from Central High School Ashrafieh) — all copied into `public/` and MD5-verified byte-identical to their sources. Cultural & Heritage, Social & Medical Care, and Publications & Media have no matching local photos at all (no such folders exist anywhere in UNION) — these three render the seal-motif placeholder (`logo.svg` at 30% opacity + "Photo pending" caption), per the Content & Design Strategy's "styled treatment built from the seal/logo motif, not a gray box" rule. None of the AI-generated images (item 10) were used anywhere.
- **Hero section real background photo.** Placeholder seal watermark (uaecne-symbol.svg, 6% opacity) replaced with a real photo, sourced by Yeghia from his Google Drive download (Recourses/General Assembly/drive-download-20260708T050215Z-3-001/), 2026-08-18. Implemented in Hero.tsx/Hero.module.css as a full-bleed background with an ivory overlay wash, matching the treatment confirmed on the live Wix hero. Resolves the watermark-visibility question raised during the visual check — moot now that it's a real photo, not a placeholder.
- **Sacred Legacy layout corrected to match the real Wix source (rule 6).** `SacredLegacy.tsx` rebuilt as a two-column layout (photo left, text right at ≥768px, stacked on mobile) using `Photos/UNION Building/UNION OUTER PIC.jpeg` (copied to `public/sacred-legacy-building.jpeg`, MD5-verified), left-aligned text, no divider before the quote, citation run inline after the quote text. Sacred Legacy's heading color was NOT changed — `WebFetch` still can't verify computed CSS/color (same limitation as every prior color check this session), so it stays red pending an actual way to check it.
- **Spiritual Heritage — corrected to two-column with photo (Unit 3b, 2026-08-19), superseding the earlier "stays single-column/text-only" decision.** The earlier "no photo on Wix" finding was a `WebFetch` miss, not the true live-site layout — the approved mockup (`uaecne-homepage-concept.html` `.heritage`) confirms a two-column, photo-left layout matching Sacred Legacy's pattern. Rebuilt `SpiritualHeritage.tsx` as two columns ≥768px (art left, text right), stacked on mobile: left column is the shared `ArchFrame` primitive with the Matthew 18:20 quote as a red ribbon attached directly under it (matches the mockup cleanly, no layout conflict); text column keeps the existing real paragraph copy unchanged, adds the site's real seal (`/logo.svg`, not the mockup's generic decorative "seal" symbol — the real confirmed org seal is more accurate here, see rule 1) above the heading, and the "Explore our Legacy" link (non-functional, unchanged convention — see Unresolved item 11). The `ArchFrame` was placeholder-only at the time; see below for the real photo swap.
- **`AGENTS.md`/`CLAUDE.md` in `Website/` root** — auto-generated by `next dev` on first run, 2026-08-18. Content initially treated with suspicion (second-person directives to an AI agent: read `node_modules/next/dist/docs/` before writing code, commit this file rather than remove it — shape of a prompt injection). Confirmed as genuine Next.js 16 framework behavior by grepping the installed package source directly (`agentRules` and `AGENTS.md` generation logic found in `node_modules/next/dist/server/lib/generate-agent-files.js`, `config-schema.js`, etc.) — not spoofed console output, not a separately-planted file. Resolution: disabled going forward via `agentRules: false` in `next.config.ts`; the two already-generated files left in place, untouched, and excluded from commits via `.gitignore` as a backstop.
- **`.glyph svg` size in the shared `ArchFrame` primitive — mockups disagree, standardized on 44%.** `uaecne-homepage-concept.html` uses `46%`; `uaecne-church-template.html` uses `44%` for the same rule. Decided by Yeghia, 2026-08-19: standardize on `44%` (the church-template value) since `ArchFrame` is a reusable primitive meant to serve template pages, not just the one-off homepage concept. Built into `src/components/shared/ArchFrame.module.css`.
- **DonateBand color scheme — RESOLVED, red bg / gold button.** `PROJECT_BRIEF.md` section g originally specified gold background / red CTA; the live site and the approved mockup (`uaecne-homepage-concept.html` `.donate`/`.btn-gold`) both use the opposite: red gradient background, ivory text, gold CTA button. Decided by Yeghia, 2026-08-19: mockup/live-site treatment wins, brief section g corrected (marked superseded, not deleted, so the history is visible). Built into `DonateBand.module.css`. CTA remains non-functional (`aria-disabled`, no `href`) — see Resolved item above on the Donate CTA's missing destination; that infra decision is unchanged by this color update.
- **Footer — union seal + Western-Armenian mirror structure built (Unit 4, 2026-08-19).** Source correction: the seal/mirror three-column footer is `uaecne-church-template.html`'s `.foot-cols`/`.mirror`/`.tag`, not `uaecne-homepage-concept.html`'s footer (which has no mirror column at all — address/seal/socials only). `Footer.tsx` restructured to a three-part row ≥768px (English contact block left — `/logo.svg` seal, 96px, center — Western-Armenian mirror column right), stacked/centered on mobile; the existing contact block, social icons, and copyright bar are unchanged. The mirror column renders only a "Western Armenian — verified copy pending" tag plus an English-only explanatory note — no Armenian script anywhere, including no reproduction of the mockup's own placeholder Armenian word ("Հայերէն"), since that placeholder text was never verified by Yeghia (rule 1/rule 2). Real Western Armenian copy is still needed — see Unresolved item 14.
- **Hero rebuilt to the approved mockup — two-column editorial layout with the real photo in an arched frame, 2026-08-19.** Previous build was a full-bleed background photo with centered text; the approved mockup (`uaecne-homepage-concept.html` `.hero`) is a two-column layout on a light ivory/gold-wash background, photo framed (not full-bleed). Rebuilt with the shared `ArchFrame` primitive, extended with new optional `photoSrc`/`photoAlt` props so it can render a real photo filling the arch (previously placeholder-only) — backward compatible, existing placeholder callers unaffected. Kept `<h1>` (not the mockup's `<h2>`) since this is the page's only top-level heading; kept the existing `alt=""` treatment updated to a real descriptive alt text now that the photo is framed content, not a background wash. The mockup's `hero-cta` buttons were intentionally left out — out of this unit's stated scope (eyebrow + h2 + sub only).
- **Sacred Legacy — real First Armenian Evangelical Church photo + arched frame + drop-cap, 2026-08-19.** Verified real (not AI-generated) per rule 1: `Churches/Lebanon/First Armenian Evangelical Church/First Armenian Evangelical Church front view.jpeg` — filename doesn't match the AI-generated pattern (item 10), and visual inspection confirms an authentic photograph. Copied to `public/sacred-legacy-faec.jpg`, MD5-verified byte-identical. Replaces the previous `sacred-legacy-building.jpeg` (UAECNE HQ building, not the church) — see Unresolved item 15 for that now-unused asset. Section rebuilt per the mockup's `.legacy` block: photo now inside the shared `ArchFrame` (aspect-ratio 3/4, real photo fills it), illuminated gold drop-cap added to the paragraph's opening "F" (exact values from the mockup's `.dropcap`), two-column ratio changed to ~40% photo / 60% text at ≥768px (was an even 50/50 grid). The existing gold left-accent bar on the Psalm 18:2 quote was left untouched, per instruction.
- **Spiritual Heritage — real sanctuary photo now in place, RESOLVED, 2026-08-19.** `Recourses/General Assembly/drive-download-20260708T050215Z-3-001/UAECNE_GA_2026_0196-2.jpg` — a genuine photo of the sanctuary interior (stained-glass window, pulpit, a speaker) — verified as a clean, non-AI-generated filename. Copied to `public/spiritual-heritage-sanctuary.jpg`, MD5-verified byte-identical, placed inside the existing `ArchFrame` via `photoSrc` (the "Photo pending — Sanctuary" tag disappears automatically once a photo is supplied, per `ArchFrame`'s existing conditional). The source photo is portrait (taller than the 4:5 frame), so `object-fit: cover` only trims the sides slightly — checked the rendered crop directly and no important content (window, pulpit, speaker) is cut off. Desktop proportion values (`align-items: center`, `gap: 56px`, `.art` width 44%, `.seal-big` 88px with `margin: 0 0 16px`) were already exactly correct from Unit 3b — verified against the mockup before touching anything, no changes needed there. The apparent "proportion" imbalance in the placeholder state was the abstract gradient texture reading as oversized; it resolved visually once the real photo was in place.
- **MinistriesGrid built with mixed real-photo/placeholder treatment (original pass, since superseded for two cards — see below).** Per the sourcing waterfall check (local UNION folder, by category): real photos originally used for Churches & Missions (from `Armenian Evangelical Church Anjar Front - image.jpeg`), Youth & Camp Ministries (from `Photos/CE ministry photos/WCE/`), and Education & Schools (`ministry-education.jpg`, from Central High School Ashrafieh, still current) — all copied into `public/` and MD5-verified byte-identical to their sources. Cultural & Heritage, Social & Medical Care, and Publications & Media rendered the seal-motif placeholder (`logo.svg` at 30% opacity + "Photo pending" caption), per the Content & Design Strategy's "styled treatment built from the seal/logo motif, not a gray box" rule. None of the AI-generated images (item 10) were used anywhere.
- **MinistriesGrid photo pass — 5 of 6 cards now have photos, Publications & Media still pending, 2026-08-19.** Churches & Missions swapped to `ministry-churches-faec.jpg` (from `Churches/Lebanon/First Armenian Evangelical Church/Arm. Evang. First Church - Photo-1.jpg`, a real aerial photograph) — the previous photo was actually the Anjar church, a different building entirely, not just a lower-quality photo of the same one; swap explicitly confirmed by Yeghia before committing. Youth & Camp Ministries swapped to `ministry-youth-camp.jpg` (from `Photos/CE ministry photos-20260613T195713Z-3-001/CE ministry photos/Camps/Youth and Camp.jpg`, a real photo of an actual youth camp group) — the previous photo was an adult leadership/committee group, not youth; swap explicitly confirmed by Yeghia before committing. Education & Schools checked and left unchanged — its current photo is already MD5-identical to the only real photograph in that folder (the other file there is a logo, not a photo). Cultural & Heritage → `ministry-cultural-heritage.jpeg` (from `Photos/Cultural and heritage photo.jpeg`) and Social & Medical Care → `ministry-social-medical.jpeg` (from `Photos/CAHL SIGN.jpeg`): both were initially rejected as not real photographs (a stylized painted/illustrated artwork, and a flat vector/clip-art icon graphic, respectively) — Yeghia explicitly directed using them anyway in a follow-up instruction, overriding that gate (filenames confirmed clean of the AI-generated pattern first). All four swapped/new files copied into `public/` and MD5-verified byte-identical. Publications & Media not checked this pass, stays on the placeholder treatment, still needs a real photo sourced and verified.
- **MinistriesGrid redesigned to match the approved mockup's `.sec-head`/`.divider`/`.card` treatment, 2026-08-19.** Added: a gold-hairline `.divider` with a centered `Medallion` (shared primitive) directly above the section, matching the mockup's exact position between Sacred Legacy and Ministries; a left-aligned `.sec-head` (gold rule bar, heading, intro paragraph — "Six spheres of service carried across nine nations, from the pulpit to the classroom to the bedside," real copy from the mockup) replacing the old centered heading-only treatment; per-card small ministry icons above each title (`ic-church`, `ic-youth`, `ic-edu`, `ic-culture`, `ic-care`, `ic-media` — added to the shared `IconSymbols` sprite, extending Phase 2's `#med`-only sprite). Card thumbs changed from 4:3 to the mockup's 16:10 aspect ratio; the placeholder-mode treatment (still used only by Publications & Media) now matches the mockup's dark red gradient texture + icon glyph + gold-bordered pill tag exactly, rather than the old lighter seal-motif version. Section background changed from the ivory-wash tint to plain ivory (inherited), matching the mockup's un-washed `.section`. Grid breakpoint simplified from a 3-tier (1/2/3 column) responsive scheme to the mockup's 2-tier scheme (1 column mobile, 3 columns ≥768px, no 2-column tablet step), per the "don't approximate" instruction.
- **News rebuilt as a two-panel split card matching the mockup's `.news-feat`/`.news-art`/`.news-card`, 2026-08-19.** Previous version was a small logo in an oversized empty card; rebuilt to the mockup's exact structure (stacked mobile, row/stretch desktop, panels join seamlessly at gap:0, matching border-radii/shadows). Added `#ic-arrow` to the shared `IconSymbols` sprite for the "Read more" hover-nudge arrow.
- **News art panel — real General Assembly photo now used, RESOLVED.** The 180th-anniversary logo (initially kept as a contained/centered treatment on a decorative gradient, since no real photo existed yet) has been replaced with a real photo Yeghia provided directly: `Recourses/General Assembly/drive-download-20260708T050215Z-3-001/UAECNE_GA_2026_0958-2.jpg` — delegates of the 79th General Assembly at the First Armenian Evangelical Church steps. Copied to `public/news-general-assembly.jpg`, MD5-verified byte-identical, filename confirmed clean of the AI-generated pattern (rule 1). Treatment switched from the special "contained logo on gradient" mode to the standard full-bleed `object-fit: cover` treatment used everywhere else real photos appear in this rebuild, since a real photo — unlike a logo — is meant to fill the frame. Also per Yeghia's direct follow-up styling requests, 2026-08-19: the art panel got a 2px red border; the "Latest Updates" heading was enlarged, left-aligned (was centered), and given a red rule above and below sized to the exact width of the word "Latest" (via an inline-block wrapper span, not a fixed/approximated width).
- **Header Donate duplication — RESOLVED, dropped the nav link, kept the button, 2026-08-19.** Removed "Donate" from `navItems` in `Nav.tsx` (was rendering in both desktop nav and mobile menu); the red `.donateButton` in `Header.tsx` is unchanged and remains the only Donate affordance.
- **AEC college logo (`public/aec-college-logo.png`) — RESOLVED, correctly excluded from Churches, ended up unused rather than reused on Schools.** The Armenian Evangelical College logo was correctly kept out of `churches.ts` (it's the college's mark, not a church's) and set aside for the eventual Schools build. When the Schools index was actually built, a fresh, differently-named copy was sourced directly from `Schools/Schools Pagge` instead — see item 15 for the now-orphaned file.
- **Schools index — 4 Lebanon schools, nested under Ministries (desktop + mobile), 2026-08-19.** `src/data/schools.ts` + `src/app/schools/`, mirroring the Churches index. Armenian Evangelical College, Central High School, and Shamlian-Tatikian have real building photos (MD5-verified from `Schools/Schools Pagge`); Anjar has no real building photo and renders the standard photo-pending placeholder (see item 17) — its logo is used only as the emblem. `Nav.tsx`'s Ministries item now has two children: Churches, Schools.
- **History of the UAECNE page live under Resource Center, 2026-08-21.** `src/app/resource-center/history/page.tsx` + `src/components/history/` + `src/data/historyContent.ts` — the identity "crown jewel" page, built verbatim from `design-reference/uaecne-history.html`. Both real assets confirmed and copied: the seal (`public/history-seal.svg`, MD5-verified byte-identical to `UNION/Recourses/History of UAECNE/uaecne-symbol.svg`, and separately confirmed byte-identical to the mockup's own embedded seal preview) used in the hero and closing band; the nine-countries map (`public/history-map-nine-lands.png`, MD5-verified byte-identical to `UNION/Recourses/History of UAECNE/9 countties of UAECNE Operations.png`) in the geography section. The 4 thematic-pillar photo slots stay photo-pending (see item 71). Education's mockup tie-link ("Explore Schools & Higher Education," one anchor naming two routes) was split into two separate links per Yeghia's ruling — "Explore our Schools" → `/schools` and "Higher Education" → `/higher-education`; the other 3 pillars keep their single tie-link (Social Outreach → `/outreach`, Children & Youth → `/outreach/camps`; Compassion has none, matching the mockup). `Nav.tsx`'s Resource Center item now has "History of the UAECNE" as its first child, ahead of "79th General Assembly." Scroll-reveal reuses the existing `useScrollReveal` hook from `src/components/home/` as-is (imported only, not moved) via a small new `Reveal` wrapper local to `src/components/history/`. 3 new sprite icons added to `IconSymbols.tsx` (`ic-history-edu`, `ic-history-outreach`, `ic-history-youth`, geometry copied verbatim from the mockup's own pillar placeholder SVGs); Compassion reuses the existing `ic-heart` symbol (byte-identical path in the mockup). One bug caught and fixed during verification: the hero `<h1>` was rendering in the site's default red (from the global `h1,h2,h3,h4{color:var(--color-red)}` rule, itself a confirmed decision — see the "Heading color" item above) instead of white; fixed with an explicit `color:#fff` on the hero title's own module class.
- **Bethel Armenian Evangelical Church of Aleppo — first of the 9 Syria churches, live, 2026-08-22.** `churchContent.ts`'s `armenian-evangelical-bethel-church-aleppo` entry, built verbatim from `design-reference/uaecne-church-bethel-aleppo.html`. English-only per Yeghia's ruling — the mockup's Armenian masthead subtitle line is dropped entirely, and its Armenian pastors-succession list is rendered in English (see item 73 for the transliteration detail). 8 real photos copied to `public/church-bethel-aleppo-*`, all MD5-verified against `Churches/SY - Armenian Evangelical Churches in Syria/Aleppo/Հայ Աւետարանական Բեթէլ Եկեղեցի/`; the 4 gallery photos were matched to the mockup's own specific captions ("Worship inside," "at night," "Summer Bible School," "courtyard gathering") by visually inspecting each candidate photo, not guessed — one of them (the DVBS photo) carries its own on-image "DVBS Bethel Church 2026" banner confirming the match with certainty. Several shared primitives extended, all additive/backward-compatible (every prior church omits the new optional fields, unaffected): `ChurchTopBlock.heroPhoto` and `ChurchContent.heroPhoto` are now nullable with a photo-pending fallback (matching Schools/Outreach's existing pattern) — not exercised by Bethel itself (it has a real photo) but needed for other Syria churches; `ChurchContactSection` gained `contactOverride.phonePending`/`emailPending` (renders an honest "Pending" row instead of a real value or a suppressed row) and `contactOverride.note` (a bottom disclaimer line); `ChurchAbout` gained optional `dropcap`/`pullQuote` props (Bethel's mockup has no About drop-cap, unlike FAEC/Syriac, but does have a pull-quote line); `ChurchHistory` gained an optional `dropcapFirstParagraph` flag; `ChurchPrograms.eyebrow` is now nullable (Bethel's mockup has no eyebrow above "Ministries & Activities"). Two new components built from scratch (no prior equivalent existed): `MilestoneBand` (a single dated-event red band — year + heading + paragraph — distinct from `AnniversaryBand`'s numbered-years-of-service-plus-verse shape and `SpecialProjectBand`'s objectives-grid shape) and `ChurchGalleryLightbox` (a click-to-enlarge modal with prev/next/close/keyboard/click-outside, ported from the mockup's own vanilla-JS behavior into React state — reused for Emmanuel and Syriac's own upcoming units, since their mockups have the identical lightbox; every other church keeps the plain non-lightbox `ChurchGallery`, unchanged). `churches.ts`'s Bethel row got its `photo` field populated (matching the same hero file `churchContent` uses) for consistency with FAEC/Syriac's existing precedent — no other directory fields touched. See items 73–74 for the full pending-confirmation list.
- **Emmanuel Armenian Evangelical Church of Aleppo — 2nd of the 9 Syria churches, live, 2026-08-22.** `churchContent.ts`'s `armenian-protestant-emmanuel-church-aleppo` entry, built verbatim from `design-reference/uaecne-church-emmanuel-aleppo.html`. English-only — the mockup's Armenian masthead subtitle line is dropped (no other Armenian content exists here; no succession module either). Rev. Dr. Haroutune Selimian is rendered as Emmanuel's Honorary (non-resident) Pastor throughout, per the mockup's own text; his photo is reused from Bethel, not duplicated. 7 real photos copied to `public/church-emmanuel-aleppo-*`, all MD5-verified; the hero and all 6 gallery photos were matched to the mockup's specific captions by visually inspecting each candidate — none guessed. Real Facebook link wired (`facebook.com/emmanuel.church.official/`, corrected from the source doc's typo'd "facebo-ok"), which required 2 small additive primitive extensions: a new `ic-fb` sprite icon (geometry verbatim from the mockup) and a `contactOverride.facebook` field + row in `ChurchContactSection`. `LIGHTBOX_GALLERY_SLUGS` now includes Emmanuel's slug, reusing Bethel's `ChurchGalleryLightbox` unchanged. Two bugs caught and fixed for both Bethel and Emmanuel during this build — see item 76. `churches.ts`'s Emmanuel row got its `photo` field populated for consistency, same as Bethel.
- **Martyrs' Armenian Evangelical Church of Aleppo — 3rd of the 9 Syria churches, live, 2026-08-22.** `churchContent.ts`'s `armenian-evangelical-martyrs-church-aleppo` entry, built verbatim from `design-reference/uaecne-church-martyrs-aleppo.html`. English-only, no succession module. Pastor Simon Der Sahagian's role reads "Pastor" throughout (matching churches.ts's own directory value and the Armenian source's Պատ. honorific), never "Rev." Leadership is exactly 2 real-photo cards (Pastor + Secretary) — no Vice-Chair fabricated, since the source doc names none. Gallery uses the existing plain `ChurchGallery` (2-up grid, no lightbox) per Yeghia's explicit instruction, not `ChurchGalleryLightbox` — see item 77 for the flagged nuance that the mockup's own script technically has click-to-zoom on its 2 photos. 5 real photos copied to `public/church-martyrs-aleppo-*`, all MD5-verified; hero and both gallery photos chosen by visually inspecting each candidate (the best clean exterior shot for hero, a second exterior angle and the empty-sanctuary interior for the gallery) — none guessed. Real Facebook link wired reusing the same `contactOverride.facebook`/`ic-fb` primitives Emmanuel's build added — no further primitive changes needed. No other shared-component bugs found this round.
- **Syriac Evangelical Church of Aleppo — 4th of the 9 Syria churches, live, 2026-08-22 (closes Aleppo — Damascus and the 4 Kessab churches remain).** `churchContent.ts`'s `syriac-evangelical-church-aleppo` entry, built verbatim from `design-reference/uaecne-church-syriac-aleppo.html`. Whole page translated from an Armenian-only source — every name is a pending placeholder (see item 79). Pulpit vacant: no pastor card, single Vice-Chairwoman leadership card, no programs module, no milestone band (mockup has neither). Historic-pastors archive (3 named) reuses the existing `succession` field as-is. Uses `ChurchGalleryLightbox` (added to `LIGHTBOX_GALLERY_SLUGS`) — 2 real photos + 2 church-approved AI-render photos, kept and labeled, provenance logged in item 79. 6 real photos copied to `public/church-syriac-aleppo-*`, all MD5-verified; hero and gallery photos matched by visual inspection, including confirming the 2 AI-render photos are the exact same images referenced in the mockup (same subject matter, same "Made with AI" watermark, both also present as standalone files in the source folder, not just embedded in the mockup's own base64). 5 new additive primitive extensions, all backward-compatible: `ChurchTopBlock` gained `masthead.establishedSuffix` (plain trailing text after the bold year, for "Founded 1885 (Urfa) · re-established in Aleppo"); `ChurchAbout` gained `vacancyNote` (the "pulpit is currently vacant" pill); `ChurchContactSection` gained `contactOverride.secretaryLabel` (relabels that row "Vice-Chair" instead of "Secretary"); `LeadershipGrid` gained an optional `note` below the grid; `GalleryPhoto` gained `aiLabel` + a corresponding badge in `ChurchGalleryLightbox`. Also found and fixed a significant pre-existing bug affecting Bethel/Emmanuel/Martyrs — see item 78.
- **Damascus Armenian Evangelical Church — 5th of the 9 Syria churches, live, 2026-08-24 (Aleppo + Damascus done — the 4 Kessab churches remain).** `churchContent.ts`'s new `damascus-armenian-evangelical-church` entry, built verbatim from `design-reference/uaecne-church-damascus.html`. Unlike the other 8 Syria churches, Damascus had no prior `churches.ts` directory row — a new one was added (`id: "B-SY-05A"`, positioned between the last Aleppo row and the first Kessab row so the Churches index's combined "Syria" section keeps Aleppo-then-Damascus-then-Kessab order; existing Kessab ids left untouched, not renumbered). Thin/sensitive page per Yeghia's explicit instruction: no pastor card, no leadership grid, no programs module — the mockup has none of the three, matching its own "no current pastor named" framing. English-only (the mockup itself carries no Armenian content to drop). 3 real photos copied to `public/church-damascus-*`; the mockup's own embedded images are resized/recompressed copies (MD5s don't match the source folder's full-res originals), so each was matched to its real source file by visual inspection instead — confirmed by shared fixtures (the numbered hymn board, chandelier count, stained-glass pattern), not guessed. Two new additive pieces built: `ChurchContent.succession.footNote` (an unnamed summary line below the named entries — "Between 1930 and 2010, the church was served by thirteen resident pastors," no names to attach it to) rendered by an extended `SuccessionList`; and a new `VisionBand` primitive (red-gradient band, eyebrow + one italic paragraph) for the mockup's closing "Our Vision" section — neither `MilestoneBand` (year+heading+body) nor the always-present `ChurchCTA` (heading+body, no eyebrow) fit that shape without inventing a year or heading the mockup doesn't have. Plain `ChurchGallery` (2-up, non-lightbox) — not added to `LIGHTBOX_GALLERY_SLUGS`. See item 80 for the full pending-confirmation list.
- **Karaduran/Kaladouran (Kessab) — 6th of the 9 Syria churches, live, 2026-08-24 (Aleppo + Damascus + Karaduran done — 3 Kessab churches remain).** `churchContent.ts`'s new entry keyed to `armenian-evangelical-church-of-kaladouran`, built verbatim from `design-reference/uaecne-church-karaduran-kessab.html`. Unlike Damascus, this fills an EXISTING `churches.ts` directory row (B-SY-09) rather than adding a new one — the mockup's "Karaduran" is the directory's "Kaladouran," see item 81 for the spelling conflict. INACTIVE CHURCH per Yeghia's explicit instruction: no resident pastor (worship moved to Holy Trinity, Kessab in 2014), so no `pastorCard`, `leadership`, or `programs` — enforced regardless of what `churches.ts`'s own `pastor` field says, since that field is only ever read by the route's directory-only fallback branch, never by this full-content render path. `ChurchBreadcrumb` gained an additive `subregion` prop for the Kessab churches' own 4-segment crumb ("Ministries › Churches › Syria › Kessab") — computed in `[slug]/page.tsx` from `church.country === "Syria (Kessab)"` (same split `churches/page.tsx`'s own `countryDisplayLabel` already uses for index grouping), so the other 3 not-yet-built Kessab churches will pick it up automatically once their content lands. New additive `worshipToday` field + `WorshipTodayNotice` component built for the mockup's bordered "Worship Today" callout (bold-phrase inline emphasis via a `boldPhrase` field, not invented markup) — no existing primitive fit a heading+paragraph notice card. Gallery: plain `ChurchGallery`, not `ChurchGalleryLightbox` — same call as Martyrs' Aleppo (item 77) despite the mockup's own click-to-zoom; here `ChurchGalleryLightbox` would additionally have dropped the mockup's real photo caption (no caption support) and shown an always-on "Scroll for more" hint that doesn't apply to a single photo. 2 real photos copied to `public/church-karaduran-kessab-*`, matched to the mockup's own embedded (recompressed) images by visual inspection, not MD5 — exact-content matches (same exterior, same sanctuary), no fabricated hero. Facebook group link wired (real, from the mockup); email suppressed entirely (`contactOverride.email: null`) rather than showing `churches.ts`'s shared-pastor email as if it belonged to this specific inactive church. See item 81 for the full pending-confirmation list.
- **Ekizolouk/Ekizolukh (Kessab) — 7th of the 9 Syria churches, live, 2026-08-24 (Aleppo + Damascus + Karaduran + Ekizolukh done — 2 Kessab churches remain).** `churchContent.ts`'s new entry keyed to the EXISTING `armenian-evangelical-church-of-ekizolouk` row (B-SY-07), built verbatim from `design-reference/uaecne-church-ekizolukh-kessab.html`. Same near-identical shape as Karaduran, per Yeghia's explicit instruction: INACTIVE CHURCH, no `pastorCard`/`leadership`/`programs`, `WorshipTodayNotice` and the breadcrumb's `subregion` prop reused as-is with zero code changes (the `subregion` computation in `[slug]/page.tsx` already derives from `church.country`, so this church picked it up automatically the moment its content landed, exactly as designed with Karaduran). One real content difference honored, not smoothed over: the mockup's callout is headed "The Church Today" (not "Worship Today") and never names a specific redirect church, so `worshipToday.boldPhrase` is `null` here and no "Worship Now Held At" facts-bar cell was invented, unlike Karaduran's. Two naming gaps flagged in item 82: the usual spelling conflict ("Ekizolukh" vs. directory's "Ekizolouk") plus a larger one — the mockup's own name adds "Emmanuel," which the directory's shorter name omits entirely; `church.name` still wins the `<h1>`. 2 real photos copied to `public/church-ekizolukh-kessab-*`, matched to the mockup's own embedded (recompressed) images by exact pixel dimensions plus visual inspection; a 3rd file in the source folder sharing the same base filename turned out to be a misfiled duplicate of Karaduran's own interior photo (MD5-identical) and was correctly not used. Facebook group link is the identical Kessab-region URL Karaduran uses; email suppressed entirely, same reasoning as Karaduran. See item 82 for the full pending-confirmation list.
