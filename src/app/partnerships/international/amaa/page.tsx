import type { Metadata } from "next";
import ComingSoon from "@/components/shared/ComingSoon";

export const metadata: Metadata = {
  title: `AMAA — UAECNE`,
};

export default function AmaaPage() {
  return <ComingSoon name="AMAA" />;
}
